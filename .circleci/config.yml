version: 2
defaults: &defaults
  docker:
    - image: node:latest
  working_directory: ~/repo

restore-npm-cache: &restore-npm-cache
  keys:
    - dependencies-{{ checksum "yarn.lock" }}-7c16afd2
    - dependencies-f54ec6eb

save-npm-cache: &save-npm-cache
  paths:
    - node_modules
    - flow-typed/npm
    - ~/.flow-typed
  key: dependencies-{{ checksum "yarn.lock" }}-7c16afd2

install: &install
  name: Install packages
  command: yarn install:all

jobs:
  basic:
    <<: *defaults
    steps:
      - checkout
      - restore_cache: *restore-npm-cache
      - run: *install
      - run:
          name: Install flow-typed
          command: yarn miko flow-typed:install
      - save_cache: *save-npm-cache
      - run:
          name: Flow
          command: yarn miko flow
      - run:
          name: Check code style
          command: yarn miko lint

  test:
    <<: *defaults
    steps:
      - checkout
      - restore_cache: *restore-npm-cache
      - run: *install
      - run:
          name: Install watchman
          command: ./.circleci/watchman.sh
      - run:
          name: Test
          command: yarn miko jest -i
      - store_artifacts:
          path: coverage

  deploy:
    <<: *defaults
    steps:
      - checkout
      - restore_cache: *restore-npm-cache
      - run: *install
      - run:
          name: Set npm key
          command: echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" > ~/.npmrc
      - run:
          name: Publish new version
          command: yarn lerna publish from-git --yes

workflows:
  version: 2
  test-and-deploy:
    jobs:
      - basic:
          filters:
            tags:
              only: /.*/

      - test:
          requires:
            - basic
          filters:
            tags:
              only: /.*/

      - deploy:
          context: global-env
          requires:
            - test
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^v(\.?\d+){3}$/
