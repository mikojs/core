// @flow

/** TODO
import path from 'path';

import { d3DirTree } from 'cat-utils';

describe('check rules amount', () => {
  const testRules = d3DirTree(path.resolve(__dirname, './__ignore__'))
    .leaves()
    .forEach(({ data }) => data.path);

  const ruleFiles = d3DirTree(path.resolve(__dirname, './../'));
});
*/
